HTML Form Input Attributes

What it is
- Input attributes control how an `<input>` behaves and what values it accepts.

Common input attributes
- `value` - sets a default value
- `readonly` - user can�t change the value
- `disabled` - input is disabled and not sent
- `size` - visible width (characters)
- `maxlength` - max number of characters
- `min` / `max` - limits for numbers, dates, and ranges
- `step` - legal number intervals
- `multiple` - allow multiple values (email or file)
- `pattern` - regular expression validation
- `required` - must be filled before submit
- `placeholder` - hint text
- `autofocus` - focus when page loads
- `autocomplete` - browser autocomplete on/off

Best example
```
<form action="/action_page.php">
  <label for="fname">First name:</label>
  <input type="text" id="fname" name="fname" value="John" size="20" maxlength="10">

  <label for="quantity">Quantity (1-5):</label>
  <input type="number" id="quantity" name="quantity" min="1" max="5" required>

  <label for="files">Select files:</label>
  <input type="file" id="files" name="files" multiple>

  <input type="submit" value="Submit">
</form>
```

Notes
- Use `required` for simple client-side validation.
- `readonly` values are submitted; `disabled` values are not.
