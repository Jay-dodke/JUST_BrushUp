CSS @supports Rule

- @supports is a feature query that checks if a browser supports a CSS property/value.
- it allows you to provide a fallback for older browsers.

What this example shows
- default layout uses flexbox.
- if grid is supported, @supports switches to grid.

Syntax
```
@supports (display: grid) {
  .layout { display: grid; }
}
```
